# Copy to .env and fill in your values
QUEUE_HOST=YOUR_DO_IP
QUEUE_PORT=9999
QUEUE_POLL=2

# Worker only: sqlite DB paths (comma-separated name=path pairs)
# QUEUE_DBS=main=/path/to/main.db,logs=/path/to/logs.db
